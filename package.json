{
  "name": "browseros",
  "version": "0.1.0",
  "description": "BrowserOS / PipeBuilder — userscript-based personal middleware system",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "vitest": "^1.3.0"
  }
}
